function one() {
    const username = "vishal pandey";
    function two() {
        const website = "youtube.com";
        // console.log(website)
    }
    two();
    // console.log(username);
}
one()

//second method store value of function into a variable

const two = function one() {
    const username ="ajay";
    function three() {
        const website = "nasa.com";
        console.log(username);
    }
    // three()
}
// one();
// console.log(two);

const add_Num = function (num) {
      return num+4;
}
console.log(add_Num(2));
